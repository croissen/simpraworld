import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simpraworld.universe',
  appName: 'Simpra Universe',
  webDir: 'dist-app'
};

export default config;
